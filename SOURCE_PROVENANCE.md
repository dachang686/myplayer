# 来源与授权边界

## 当前运行入口

`index.html` 已切换为与 `clean-room.html` 内容一致的清洁入口。两个入口只加载本仓库
的原创本地脚本，不加载外部脚本、网页字体、真人头像或现实球队标志。页面结构、样式
和 `js/clean_room_game.js` 游戏控制逻辑均在本项目中独立编写。

- 城市：采用公开的真实地理名称，仅表示比赛所在地，不代表任何现实球队或组织。
- 联盟和球队昵称：项目内人工编写的中式俱乐部风格虚构设定，不复制参考游戏、现实
  球队或真实赞助商的名称与标志，也不表示任何商业赞助关系。
- 球员显示名：仅采用公开的中文姓氏译名，不包含名字、完整姓名、照片、肖像或签名；
  连字符复姓保留完整形式。相同姓氏允许重复，内部球员标识仍保持唯一。
- 命名边界：姓氏仅作为界面显示标签，不主张现实运动员参与、认可或授权本项目。
- 位置和能力值：按用户要求保留原名单中与姓氏对应的位置、总评及 13 项能力数值；
  不保留身高、年龄、完整姓名、英文类型文案、统计履历或现实赛程。
- 赛程：项目内生成，不对应现实赛季。
- 生涯文案：项目内重新编写的通用虚构联盟文案。
- 运行依赖：浏览器原生 HTML、CSS、JavaScript API，无运行时第三方包。

## 清理记录

2026-08-09 完成默认入口切换，并从工作树移除旧活动素材目录、历史脚本、脚本归档、
抽取工具及其专用依赖，共 688 个受版本控制的文件。删除内容可以从版本历史恢复，但
不再存在于当前项目文件中。

## 自动检查

```bash
node scripts/check_legal_surface.js
```

该命令检查实际运行入口及其脚本依赖，阻止第三方域名、旧数据模块、列明的真人完整姓名、
现实联盟/游戏品牌和旧 `assets/` 引用重新进入运行面。

```bash
node scripts/check_legal_surface.js --repository
```

严格模式还要求默认入口与清洁入口完全一致，旧素材目录、历史镜像脚本、归档及抽取工具
已从仓库删除，并对仓库内其余文本文件执行同一组禁用内容检查。

## 限制

本文件用于记录工程来源和自动检查范围，不是法律意见，也不能替代发布前由具备相应
资质的专业人员结合发行地区、名称检索、宣传材料和实际使用方式所做的审查。
